fn test(int x) -> int {
    if (x > 0) {
        return 1;
    } else {
        if (x < 0) {
            return -1;
        } else {
            return 0;
        }
    }
}

fn main() -> int {
    return test(5);
}
