fn test(int x) -> int {
    int a = x + 0;
    int b = 0 + x;
    int c = x * 1;
    int d = 1 * x;
    int e = x * 0;
    int f = 0 * x;
    int g = x - 0;
    int h = x - x;
    int i = x / 1;
    return a + b + c + d + e + f + g + h + i;
}

fn main() -> int {
    return test(10);
}
